/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'zxirxwpvltpkcookosxz.supabase.co',
      },
    ],
  },
};

export default nextConfig;
